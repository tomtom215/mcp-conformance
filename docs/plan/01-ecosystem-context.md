<!-- SPDX-License-Identifier: MIT -->
<!-- Copyright 2026 Tom F. (https://github.com/tomtom215) -->

# Ecosystem Context — Verified Facts Register

**Status:** Active
**Last reviewed:** 2026-07-26

---

This register is the **only** place in the plan where volatile external facts (versions,
download counts, tier tables, dates) are recorded. Every row carries the date it was last
verified against the listed source. Other documents link here instead of repeating numbers.

**Maintenance rules**

1. A row older than 90 days must be re-verified before it is cited in anything external
   (a README claim, an upstream issue, a report).
2. Re-verification updates the row in place — value, date, and source. Git history preserves
   the old value; the document does not.
3. A refuted row is corrected immediately and anything derived from it is re-examined; the
   derivation chain is what the `Used by` column tracks.
4. Facts with no bearing on engineering decisions do not belong here.

Statuses: **Verified** (primary source fetched on the stated date) ·
**Partial** (true with stated nuance) · **Unverified** (recorded for follow-up; must not be
cited).

## 1. Protocol and governance

| # | Fact | Status | Verified | Source | Used by |
|---|------|--------|----------|--------|---------|
| 1.1 | Current MCP protocol revision is `2025-11-25` | Verified | 2026-06-09 | [Versioning](https://modelcontextprotocol.io/specification/versioning) | Charter, Architecture |
| 1.2 | A release candidate dated `2026-07-28` exists; final ships 2026-07-28 | Verified | 2026-06-09 | [RC announcement](https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/) (2026-05-21) | Architecture, Roadmap, Risks |
| 1.3 | The 2026-07-28 RC removes the `initialize`/`initialized` handshake and the `Mcp-Session-Id` header ("stateless rework") | Verified | 2026-06-09 | RC announcement: "The `initialize`/`initialized` handshake is removed." / "The `Mcp-Session-Id` header … also removed." | Architecture §state machines |
| 1.4 | Feature lifecycle policy: deprecated features remain ≥ 12 months before removal, with a 90-day expedited-removal exception | Verified | 2026-06-09 | Versioning page; RC announcement | Standards §deprecation |
| 1.5 | The RC adds an Extensions framework: reverse-DNS IDs negotiated via an `extensions` capability map | Verified | 2026-06-09 | RC announcement | Conformance strategy |
| 1.5a | Draft change inventory with SEP attributions, reconciled against the **draft changelog** (the 2026-05-21 RC announcement enumerated only part of this; the changelog is the authoritative list). **Major:** protocol sessions and `Mcp-Session-Id` removed — list endpoints no longer vary per-connection, cross-call state via server-minted handles passed as ordinary tool arguments (SEP-2567) · stateless lifecycle: `initialize`/`initialized` handshake removed, every request carries `io.modelcontextprotocol/{protocolVersion, clientInfo, clientCapabilities}` in `_meta`, mismatches return `UnsupportedProtocolVersionError` (SEP-2575) · `server/discover`: "servers MUST implement this RPC to advertise their supported protocol versions, capabilities, and identity"; clients MAY call it first or use it as a backcompat probe on stdio (SEP-2575) · `subscriptions/listen` — a single long-lived POST-response stream with per-type opt-in (`toolsListChanged`, `promptsListChanged`, `resourcesListChanged`, `resourceSubscriptions`) and `io.modelcontextprotocol/subscriptionId` tagging — replaces the HTTP GET endpoint and `resources/subscribe`/`unsubscribe`; request-scoped notifications (`progress`, `message`) stay on their request's response stream (SEP-2575) · `ping`, `logging/setLevel`, `notifications/roots/list_changed` removed; log level set per-request via `_meta` `io.modelcontextprotocol/logLevel`, and "servers MUST NOT emit `notifications/message` for requests that did not include this field" (SEP-2575) · tasks leave core for the official `io.modelcontextprotocol/tasks` extension: blocking `tasks/result` → polling `tasks/get` plus new `tasks/update`, `tasks/list` removed, unsolicited task handles allowed (SEP-2663) · **MRTR** (Multi Round-Trip Requests) replaces server-initiated requests (`roots/list`, `sampling/createMessage`, `elicitation/create`): servers return `inputRequests`, clients answer via `inputResponses` on the next request (SEP-2322). **Minor:** `extensions` capability field on both sides · required `Mcp-Method`/`Mcp-Name` Streamable-HTTP headers plus custom headers from tool parameters via `x-mcp-header` (SEP-2243) · `inputSchema`/`outputSchema` loosened to full JSON Schema 2020-12 with `$ref` resolution requirements and composition-keyword resource bounds (SEP-2106) · missing-resource error code `-32002` → JSON-RPC standard `-32602` (SEP-2164) · `ttlMs`/`cacheScope` **required** via `CacheableResult` on `tools/list`, `prompts/list`, `resources/list`, `resources/read`, `resources/templates/list` (SEP-2549) · W3C Trace Context conventions for `_meta` (SEP-414) · deterministic `tools/list` ordering SHOULD · authorization (entered the changelog mid-window via PR #2862, merged 2026-06-05): authorization servers SHOULD send `iss` and clients MUST validate a present one (RFC 9207, SEP-2468); appropriate `application_type` required during DCR (SEP-837); credentials issuer-bound — keyed by issuer, never reused across authorization servers (SEP-2352). The announcement's window framing stands: the **ten-week** window "is for SDK maintainers and client implementers to validate the changes against real workloads"; "A Standards Track SEP can no longer reach Final status until a matching scenario lands in the conformance suite" | Verified | 2026-06-11 | [Draft changelog](https://modelcontextprotocol.io/specification/draft/changelog) (raw fetched 2026-06-11); [RC announcement](https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/); [PR #2862](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2862) | Roadmap (M2.5 extraction checklist), Risks R1 |
| 1.5b | Draft **deprecations** (changelog §Deprecated; lifecycle policy per SEP-2596 — row 1.4): **Roots, Sampling, and Logging deprecated** — "remain fully functional during the deprecation window but new implementations should not add support for them"; suggested migrations: tool parameters / resource URIs / server configuration for roots, direct LLM-provider integration for sampling, stderr or OpenTelemetry for logging (SEP-2577) · HTTP+SSE transport reclassified Deprecated under the lifecycle policy (SEP-2596) · `includeContext` values `"thisServer"`/`"allServers"` reclassified Deprecated, removal "no later than the Sampling feature itself" (SEP-2596) · OAuth **Dynamic Client Registration (RFC 7591) deprecated** in favor of Client ID Metadata Documents, retained for backwards compatibility (PR #2858; changelog entry added mid-window by PR #2862, 2026-06-05) · a [deprecated-features registry page](https://modelcontextprotocol.io/specification/draft/deprecated) now tracks every Deprecated feature | Verified | 2026-06-11 | [Draft changelog](https://modelcontextprotocol.io/specification/draft/changelog) §Deprecated; [PR #2862 diff](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2862) | Roadmap (M2.5 checklist; M3 scope — sampling/roots/logging stay active in `2025-11-25` but are deprecated forward), Risks R1 |
| 1.5c | RC-tracking re-reconciliation (2026-06-14) against the live draft changelog: rows 1.5a/1.5b's protocol change inventory is **unchanged** — all seven Major, ten Minor, and four Deprecated items present and consistent, so the M2.5 extraction checklist (roadmap line 2) is confirmed current. Two **new, non-protocol-behavioral** entries have appeared since the 2026-06-11 fetch: a `schema.json` generator correctness fix — it "now correctly reflects that the Typescript definition of minimum/maximum/default are `number`'s and not just `integers`" (changelog §"Other schema changes", PR #2710) — and the formalization to "PR-based SEP workflow with markdown files in `seps/` directory, PR-derived numbering, sponsor responsibilities, and status management via PR labels" (changelog §"Process changes", SEP-1850). Neither adds a registry-extraction target (a schema-generation fix; an authoring-process change). Two **citation-only** shifts, inventory unaffected: the resource-not-found error-code change (`-32002` → `-32602`, 1.5a) now carries **no SEP citation** in the changelog (1.5a attributes it to SEP-2164; the change persists as Minor #6), and the authorization deltas (SEP-2468/SEP-837/SEP-2352) now stand on their own SEPs rather than via the integrating PR #2862 (1.5a). **Non-finding, recorded so it is not re-chased:** the changelog's `…/modelcontextprotocol/specification/…` PR and compare links are GitHub **redirect aliases** (HTTP 301) to the canonical `…/modelcontextprotocol/modelcontextprotocol` repo — the raw changelog is byte-identical at both paths — not a spec-repo migration | Verified | 2026-06-14 | [Draft changelog](https://modelcontextprotocol.io/specification/draft/changelog) (raw fetched 2026-06-14 from `…/modelcontextprotocol/modelcontextprotocol/main/docs/specification/draft/changelog.mdx`); [PR #2710](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2710); [SEP-1850 PR](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/1850) | Roadmap (M2.5 checklist — confirmed current), Risks R1 (no new scope) |
| 1.5d | RC-tracking re-reconciliation (2026-06-27) against the live draft changelog: the inventory has **grown by four entries** since the 2026-06-14 reconciliation (row 1.5c), so the M2.5 extraction checklist (roadmap line 2) is **extended**. This is a true delta, not a prior miss — `main` HEAD `20136c2` (latest on/before 2026-06-14) carried **seven** Major items, today's `main` carries **nine**; the resultType/SSE-removal entries were authored 2026-06-07 on a feature branch (commit `0fc9d5a`) and merged to `main` after the 1.5c fetch, alongside the 2026-06-12/-17 error-code and elicitation changes. Two new **Major** entries: (8) **all results carry a required `resultType` field** — "`complete`" for ordinary results, "`input_required`" for MRTR interim results, and "Clients **MUST** treat results from earlier-protocol servers that omit the field as `complete`" (SEP-2322); (9) **SSE stream resumability and message redelivery removed from Streamable HTTP** — verbatim: "Remove SSE stream resumability and message redelivery (the `Last-Event-ID` header and SSE event IDs) from the Streamable HTTP transport. A broken response stream loses the in-flight request; clients **MUST** re-issue it as a new request with a new request ID" (SEP-2575). Two new **Minor** entries (now twelve, was ten): (11) **`notifications/elicitation/complete` and the URL-mode `elicitationId` field removed** (both introduced in `2025-11-25`) — under MRTR the client learns the outcome by retrying the original request; servers correlate across retries via `requestState`; (12) **error-code allocation policy** partitioning the JSON-RPC server-error range (`-32000`–`-32019` implementation-defined, `-32020`–`-32099` reserved for MCP) with the draft codes renumbered (`HeaderMismatch -32001→-32020`, `MissingRequiredClientCapability -32003→-32021`, `UnsupportedProtocolVersion -32004→-32022`) and `HeaderMismatchError` added to the schema. **Decision impact — Major #9 undercuts the rmcp SSE-resumption upstream filing** (row 3.12; deferral `rmcp-sse-resumption-upstream-filing`; engagement backlog item 10): the `2026-07-28` text *removes* the `Last-Event-ID` resumption the dossier asks rmcp to add to its POST path, so filing the "add resumption" fix now would ask upstream to build a mechanism the next revision deletes — the filing is re-decided (deferral re-dated to re-scope toward the durable angle: rmcp *hangs* on early POST-stream close instead of surfacing an error the caller can re-issue, which the new spec **requires**, verifiable only against the final text + the draft suite scenarios). The other three are line-2 registry-extraction targets gated on the final text. The seven prior Major, ten prior Minor, and four Deprecated items are unchanged and consistent | Verified | 2026-06-27 | [Draft changelog](https://modelcontextprotocol.io/specification/draft/changelog) (raw fetched 2026-06-27 from `…/modelcontextprotocol/modelcontextprotocol/main/docs/specification/draft/changelog.mdx`); changelog commit history (`commits?path=docs/specification/draft/changelog.mdx`: `0fc9d5a` 2026-06-07 resultType+SSE-removal, `f505a6c`/`73ab7d2` 2026-06-12 error-code, `0b7f2e4`/`380f1af` 2026-06-17 elicitation); content at `main`-on-2026-06-14 `20136c2` (seven Major) vs today (nine); [SEP-2322](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2322), [SEP-2575](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2575) | Roadmap (M2.5 checklist — extended; M4 SSE filing re-decided), Risks R1 |
| 1.5e | RC-tracking re-reconciliation (2026-07-19, 9 days before the `2026-07-28` ship): the **final text has not shipped** (`/specification/2026-07-28` → HTTP 404). Draft-changelog **item counts are stable** vs 1.5d (nine Major, twelve Minor, four Deprecated), but one Major entry's wording was **refined** since the 2026-06-27 fetch (byte-diff of the raw changelog): **Major #2 (SEP-2575 stateless)** demotes `io.modelcontextprotocol/clientInfo` from a *required* per-request `_meta` field to a **SHOULD** ("Clients **SHOULD** identify themselves on each request"), and adds a new symmetric obligation — **servers SHOULD identify themselves in each result's `_meta`** via `io.modelcontextprotocol/serverInfo`. Per-request `_meta` now carries `protocolVersion` + `clientCapabilities` (required) with `clientInfo`/`serverInfo` as SHOULDs. This is a registry-extraction refinement for M2.5 line 2 (the stateless `_meta` field requirements) and dovetails with the `from_build_env` finding (register 3.9): the new spec makes per-result server self-identification a SHOULD, which rmcp's `from_build_env` default fills with the SDK's identity, not the app's. No SEP added or removed; the other eight Major, twelve Minor, four Deprecated items are unchanged | Verified | 2026-07-19 | [Draft changelog](https://modelcontextprotocol.io/specification/draft/changelog) (raw fetched 2026-07-19); byte-diff vs the 2026-06-27 fetch; `/specification/2026-07-28` HTTP 404 | Roadmap (M2.5 checklist — refined), Risks R1 |
| 1.5f | RC-tracking re-reconciliation (2026-07-26, **two days before** the `2026-07-28` ship date): the **final text still has not shipped** — `/specification/2026-07-28` returns HTTP 404 and the spec repo's `main` carries no `docs/specification/2026-07-28/` directory (both `index.mdx` and `changelog.mdx` 404 on raw). The draft changelog is **substantively unchanged** since 1.5e: still **nine Major, twelve Minor, four Deprecated**, with Major #2 carrying the 1.5e wording (per-request `_meta` `protocolVersion` + `clientCapabilities` required; `clientInfo` and the new per-result `serverInfo` as SHOULDs). The M2.5 extraction checklist therefore stands as re-scoped on 2026-07-19 — no new inventory items in the last week of the window. **What moved is the SDK side, not the text:** the official blog shipped "Beta SDKs for the 2026-07-28 MCP Spec Release Candidate Are Here" (2026-06-29), and rmcp's `3.0.0-beta` line now implements the revision end to end (row 3.1) — a readable reference implementation of the stateless rework exists *before* the normative text does. That is leverage for M2.5 line 2 as a **cross-check only**: the extraction method takes quotes from the published spec, never from an SDK | Verified | 2026-07-26 | [Draft changelog](https://modelcontextprotocol.io/specification/draft/changelog) (raw fetched 2026-07-26; section item counts recomputed); `/specification/2026-07-28` → 404; `docs/specification/2026-07-28/{index,changelog}.mdx` → 404 on `main`; [Beta SDKs post](https://blog.modelcontextprotocol.io/) (2026-06-29) | Roadmap (M2.5 — still gated on the text), Risks R1 |
| 1.5g | **Measured** draft readiness (2026-07-26), as opposed to the inventory in 1.5a–1.5f: the official runner's `2026-07-28` scenario set (suite `0.2.0-alpha.9`) against `mcp-everything-server` at HEAD scores `1 passing / 20 failing / 1 informational across 20 scenarios`. Every failure is the *same* failure — `HTTP 422` on the runner's first request, because SEP-2575 removes the handshake and rmcp 1.7.0's streamable-HTTP server rejects a request that arrives without one, before any handler runs. No session is admitted, so the tap records nothing and no capability is actually exercised: the twenty scenario names are not twenty pieces of migration work, they are one (the stateless lifecycle) seen twenty times. The single pass is `dns-rebinding-protection / localhost-host-rebinding-rejected` — the `Host`/`Origin` policy sits in middleware ahead of protocol handling, so it is revision-independent. Committed as `conformance/draft-readiness.json` and ratcheted by the scheduled `draft-readiness` job; write-up in [docs/reports/draft-2026-07-28-readiness-2026-07-26.md](../reports/draft-2026-07-28-readiness-2026-07-26.md). **Not a conformance claim about `2026-07-28`** — the registry does not describe that revision, and the validator is deliberately not involved | Verified | 2026-07-26 | `cargo xtask draft-readiness` (reproducible; artifacts under `target/draft-readiness/`); runner error text captured first-hand | Roadmap (M2.5 sequencing), Risks R1 |
| 1.5h | **The `2026-07-28` final text SHIPPED, on its scheduled date (2026-07-28)** — reversing the standing "not shipped" finding of 1.5e/1.5f and releasing the gate M2.5 lines 2 and 5 have waited on since 2026-06-09. Confirmed four independent ways, because a dated directory appearing on `main` is on its own only *staging*: (a) `/specification` now resolves to `/specification/2026-07-28` (HTTP 200), i.e. the revision is the site default; (b) the site's `docs.json` version dropdown carries `"Version 2026-07-28 (latest)"` with `"default": true`, alongside a **separate** `Draft` entry; (c) upstream's `docs/specification/draft/changelog.mdx` has been **reset to the five-line post-release stub** ("Changes since the most recent release will accumulate here") while `docs/specification/2026-07-28/changelog.mdx` carries the full 121-line "Key Changes" — the cut fingerprint, and the reason `draft/` upstream now means the *next* revision, not this one; (d) `schema/2026-07-28/schema.ts` exists on `main`. **Inventory confirmed unchanged at ship:** the published changelog carries exactly **nine Major, twelve Minor, four Deprecated** — identical to 1.5d–1.5f — so the M2.5 extraction checklist stands as re-scoped 2026-07-19, with **no ship-day delta to absorb**. The three non-normative sections are all already accounted for and are not extraction targets: "Other schema changes" (one item, PR #2710 — row 1.5c), "Governance and process updates" (one item, SEP-2596's feature-lifecycle policy — rows 1.4/1.5b), "Process changes" (one item, SEP-1850 — row 1.5c). **The other two external gates did NOT clear, and the text shipping does not move them:** rmcp's newest is `3.0.0-beta.4` (published 2026-07-28) but `max_stable_version` is still **2.2.0**, so [ADR-0011](decisions/0011-rmcp-pin-holds-at-1-7.md)'s *second* condition (a stable `3.0.x`) is unmet and the pin holds at 1.7.0; and the official suite's npm `latest` is still **0.1.16** (unmoved since 2026-03-30) with `alpha` now at **`0.2.0-alpha.10`** (published 2026-07-27) — one ahead of `draft_readiness::DRAFT_SUITE_VERSION` (`0.2.0-alpha.9`), so the `suite-0-2-0-stable-pin-bump` deferral is unmoved and the ratchet's pin is now one alpha behind. **Non-finding, recorded so it is not re-chased:** `/specification/versioning` still reads "The **current** protocol version is **2025-11-25**" even though every link on that same page resolves to `/specification/2026-07-28` (including the deprecated-features registry it points at) — a site-side lag on a shared, unversioned page, not evidence the revision is unreleased; the dropdown default, the `/specification` redirect, and the draft-changelog reset all say otherwise | Verified | 2026-07-28 | `/specification` → `/specification/2026-07-28` (200, `curl -L -w %{url_effective}`); [`docs.json`](https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/docs/docs.json) version dropdown (raw fetched 2026-07-28); `docs/specification/{draft,2026-07-28}/changelog.mdx` raw-diffed 2026-07-28 (5 lines vs 121; section counts recomputed); `schema/2026-07-28/schema.ts` HTTP 200 on `main`; [crates.io `rmcp`](https://crates.io/api/v1/crates/rmcp) (`max_stable_version` 2.2.0, newest `3.0.0-beta.4`); [npm `@modelcontextprotocol/conformance`](https://registry.npmjs.org/@modelcontextprotocol/conformance) dist-tags (`latest` 0.1.16, `alpha` 0.2.0-alpha.10) | Roadmap (M2.5 lines 2 + 5 **unblocked**; M5's gate still compound), ADR-0011 (condition 1 met, condition 2 unmet — pin holds), Risks R1 |
| 1.5i | **Re-measured draft readiness (2026-08-17), now two-legged and no longer a proxy.** Row 1.5g's number (`1 passing / 20 failing`) described rmcp 1.7.0 rejecting a handshakeless request with HTTP 422; both inputs have moved since — rmcp 3.1.2 serves `2026-07-28` statelessly, and `mcp-everything-server` gained a `--protocol-version 2026-07-28` mode. `cargo xtask draft-readiness` now runs the suite's `2026-07-28` scenarios against **both** server modes and keys the baseline by the revision served. Result: `23 passing / 0 failing / 0 informational across 20 scenarios`, for each leg. **The runner cannot distinguish the two servers, and that is the finding.** Its scenarios exercise features — list, call, read — and rmcp answers a per-request-versioned POST whichever revision the handler advertises, so a server implementing `2025-11-25` scores the same 23/23 as one implementing `2026-07-28`. This workspace's own registry separates them: judged over the two sessions the task captures, the legacy server scores **60 pass, 1 fail** (CACH-001 — no `ttlMs` on cacheable results) and the stateless server **61 pass, 0 fail**, with **64 clauses not observed** on each. *(Corrected 2026-08-18: this row read `123 pass / 1 fail` and `124 pass / 0 fail` — the pre-[ADR-0012](decisions/0012-not-observed-outcome.md) counts, which fold not-observed into pass. The 2026-08-17 sweep that corrected this arithmetic everywhere else stopped at the CLAIM_FILES the `draft-coverage` gate parses; these plan docs are outside it, so the inflated pair survived here, in 03-conformance-strategy and in 06-roadmap. Found when a CHANGELOG entry quoted this row and the gate rejected the verdict. Re-stated 2026-08-20: the pair moved again, from `59/1` and `60/0`, because the registry gained a judged clause rather than because either server changed — `TRAN-056` (client messages travel by POST) had been excluded only because the recorded HTTP event carried no method, and now it does. The captures were re-recorded at the `0.2.0-alpha.11` pin in the same change, so they carry the method the check reads.)* Both recordings are committed under `corpus/draft/captured/` and replayed by the golden suite on every `cargo test`. **2026-08-18 — the runner can now distinguish them, and it agrees with the registry.** Bumping the ratchet to `0.2.0-alpha.11` (from `alpha.9`) added a `wire-schema-valid` check that validates every message against the negotiated revision's JSON schema. Re-measured: the `2026-07-28` leg scores **41 passing / 0 failing**, the `2025-11-25` leg **37 passing / 4 failing** — the four being `wire-schema-valid` on `resources-{list,read-text,read-binary,templates-read}`, each for `must have required property 'cacheScope'` and `'ttlMs'`. **That is CACH-001, the one clause this row already recorded the registry finding and the runner missing, found independently six weeks later.** No pre-existing check changed status: the entire delta is the 36 new `wire-schema-valid` checks, 32 of which pass. The asymmetry is instructive — `tools/list` and `prompts/list` pass because rmcp's `#[tool_handler]`/`#[prompt_handler]` expansions attach the hints unconditionally, while `resources/*` go through this workspace's revision-aware `cached()`, which correctly withholds them at `2025-11-25`; the honest implementation is the one the new check fails. The sentence above ("the runner cannot distinguish the two servers") held for `alpha.9` and is superseded here rather than deleted, because the supersession *is* the finding: a negative result about an instrument expires when the instrument improves. **Two findings withdrawn as instrument error:** the earlier capture's TRAN-058 (client POSTs missing `Mcp-Method`/`Mcp-Name`) and TRAN-068 (SSE without `X-Accel-Buffering`) were artifacts of the tap's recording allowlist, which predated SEP-2243 and dropped those headers; both were sent on the wire, and both clauses pass now that the recording keeps them. Recorded here rather than silently corrected, because it is the second capture-fidelity defect on this surface and the class is worth naming: **a check is only as honest as the recording it reads** | Verified | 2026-08-17 (runner-distinguishability re-measured 2026-08-18) | `cargo xtask draft-readiness` (reproducible; artifacts under `target/draft-readiness/<revision>/`); `conformance/draft-readiness.json`; `corpus/golden/draft/official-suite-2026-07-28-{scenarios,stateless}.json`; rmcp 3.1.2 source read for `X-Accel-Buffering` and the `Mcp-Method` rejection path | Roadmap (M2.5 sequencing, Draft-readiness workstream), Risks R1 |
| 1.6 | MCP was donated to the Agentic AI Foundation (Linux Foundation directed fund) on 2025-12-09; governance model unchanged | Verified | 2026-06-09 | [Anthropic announcement](https://www.anthropic.com/news/donating-the-model-context-protocol-and-establishing-of-the-agentic-ai-foundation); [LF press release](https://www.linuxfoundation.org/press/linux-foundation-announces-the-formation-of-the-agentic-ai-foundation); [MCP blog](https://blog.modelcontextprotocol.io/posts/2025-12-09-mcp-joins-agentic-ai-foundation/) | Engagement |
| 1.7 | Scale: "97M+ monthly SDK downloads across Python and TypeScript"; "more than 10,000 active public MCP servers" (Anthropic) / "10,000 published" (LF) | Verified | 2026-06-09 | Same as 1.6 | Charter (context) |
| 1.8 | Lead Maintainers: David Soria Parra ("Lead Core Maintainer") and Den Delimarsky; affiliations are no longer published on the governance page | Verified | 2026-06-09 | [Governance](https://modelcontextprotocol.io/community/governance); [maintainer update](https://blog.modelcontextprotocol.io/posts/2026-04-08-maintainer-update/) | Engagement |

## 2. Conformance and tiering

| # | Fact | Status | Verified | Source | Used by |
|---|------|--------|----------|--------|---------|
| 2.1 | `modelcontextprotocol/conformance` is the official suite: TypeScript, npm `@modelcontextprotocol/conformance`, tests **both** clients and servers | Verified | 2026-06-09 | [Repo README](https://github.com/modelcontextprotocol/conformance) | Conformance strategy |
| 2.2 | Server-under-test wiring: `npx @modelcontextprotocol/conformance server --url http://localhost:3000/mcp`; client-under-test: `… client --command "<cmd>" --scenario <name>` | Verified | 2026-06-09 | Repo README | Architecture §xtask, Roadmap M2/M3 |
| 2.3 | Scenario suites: `all`, `core`, `extensions`, `backcompat`, `auth`, `metadata`, `draft`, `sep-835`; a `tier-check` subcommand evaluates a repo against SEP-1730 | Verified | 2026-06-09 | Repo README | Conformance strategy |
| 2.4 | npm package: latest stable **0.1.16** (2026-03-30); **0.2.0-alpha** line active (`alpha` dist-tag → **alpha.3** as of 2026-06-12; alpha.2 was current 2026-06-11); ~20,985 downloads/month (2026-06-11). **2026-07-26: latest stable is *still* `0.1.16`** — unchanged for four months — while the `alpha` dist-tag advanced `alpha.3 → alpha.9` (alpha.4 2026-06-16, .5 06-23, .6 06-24, .7 06-25, .8 06-30, .9 2026-07-01) and had then been quiet for 25 days. **2026-08-18: that quiet claim did not survive the day it was written** — `alpha.10` published **2026-07-27**, the day after this row asserted quiet, and `alpha.11` on **2026-08-07**; the `alpha` dist-tag now reads `0.2.0-alpha.11`. This is ADR-0010's founding example recurring verbatim ("the npm `alpha` dist-tag moved within 24 hours of a row asserting it had not"), and it is why the row now states the *observation window* rather than a prediction of quiet. Latest stable is nevertheless **still `0.1.16`**, unchanged since 2026-03-30 — four and a half months. Consequence for the deferral `suite-0-2-0-stable-pin-bump` (re-decided 2026-08-18): there is still nothing stable to bump `SUITE_VERSION` *to*, so that half re-dates; the draft ratchet `DRAFT_SUITE_VERSION` moved `alpha.9 → alpha.11` in the same commit, because a readiness measure held against a six-week-old pre-release scenario set measures an old question | Verified | 2026-08-18 | [npm registry](https://registry.npmjs.org/@modelcontextprotocol%2Fconformance) (dist-tags + per-version publish times re-fetched 2026-08-18: 0.1.16 2026-03-30, alpha.9 2026-07-01, alpha.10 2026-07-27, alpha.11 2026-08-07); [downloads API](https://api.npmjs.org/downloads/point/last-month/@modelcontextprotocol/conformance) (counts 2026-06-11) | Standards §pinning, Risks |
| 2.5 | SEP-1730 (SDK Tiering) is **Final** (created 2025-10-29; authors Inna Harper, Felix Weinberger). Tier 1: "All conformance tests pass" (100%), new features inside the RC→release window — the SEP text states a "two week window between Release Candidate and the new protocol version release"; the observed 2026-07-28 cycle is running ~10 weeks (RC announced 2026-05-21, final 2026-07-28 — row 1.2), so treat two weeks as the SEP's floor, not the practiced cadence — triage within two business days, security/critical bugs resolved within seven days, stable release & versioning documented. Tier 2: 80% pass, features within six months, ≥ 1 stable release, published Tier-1 roadmap | Verified | 2026-06-09 | [SEP-1730](https://modelcontextprotocol.io/seps/1730-sdks-tiering-system); [sdk-tiers](https://modelcontextprotocol.io/community/sdk-tiers) | Charter, Conformance strategy, Roadmap |
| 2.6 | SEP-1730 appendix asks SDK maintainers to "implement everything server based on a spec … check it into each SDKs repo" | Verified | 2026-06-09 | SEP-1730 appendix | Charter, Roadmap M2 |
| 2.7 | "Critical Bug" = P0: security vulnerabilities with CVSS ≥ 7.0 or core functionality failures | Verified | 2026-06-09 | sdk-tiers page | Security model |
| 2.8 | Published tier table: **Tier 1** TypeScript, Python, C#, Go · **Tier 2** Java, **Rust** · **Tier 3** Swift, Ruby, PHP · Kotlin TBD (official tiering published 2026-02-23) | Verified | 2026-06-09 | [SDK docs](https://modelcontextprotocol.io/docs/sdk) | Charter, Engagement |
| 2.9 | SEP-2484 (merged 2026-05-17, author pcarleton): a Standards-Track SEP changing observable behavior cannot reach Final without a merged conformance scenario tagged with the SEP number **and** a traceability file (`sep-NNNN.yaml`) mapping each MUST/MUST NOT to a check or a documented exclusion | Verified | 2026-06-09 | [PR #2484](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2484) | Conformance strategy (traceability format) |
| 2.10 | The TypeScript everything server (`modelcontextprotocol/servers`, `src/everything`) exercises tools, prompts, resources (links/references/subscriptions), sampling (sync+async), elicitation (incl. URL mode), roots, logging toggles, progress, structured output | Verified | 2026-06-09 | [src/everything](https://github.com/modelcontextprotocol/servers/tree/main/src/everything) | Architecture §everything-server scope |
| 2.11 | SEP-1730 names trace validation as the long-term conformance method: "All SDKs must undergo conformance testing using protocol trace validation: for details see Conformance Testing RFC (forthcoming)" — the linked forthcoming RFC is SEP-1627 | Verified | 2026-06-09 | [SEP-1730 issue](https://github.com/modelcontextprotocol/modelcontextprotocol/issues/1730) | Charter (legitimacy anchor), Conformance strategy |
| 2.12 | SEP-1627 "Conformance Testing" (Draft, sponsor pcarleton) designs recorded "human-reviewable protocol trace 'goldens'" shared across SDKs and a "Protocol Debugger" that "Takes in a trace (set of JSON RPC Messages of an MCP connection), flags any compliance violations (broken MUSTs) warns against unimplemented recommendations (broken SHOULDs)" — design only; the shipped suite runs live scenarios | Verified | 2026-06-09 | [SEP-1627 issue](https://github.com/modelcontextprotocol/modelcontextprotocol/issues/1627) | Charter, Conformance strategy, Risks (R2 watch signal) |
| 2.13 | Suite operational pain, both open: [#182](https://github.com/modelcontextprotocol/conformance/issues/182) "tier-check reports 0/30 server conformance despite all tests passing"; [#114](https://github.com/modelcontextprotocol/conformance/issues/114) "Server lifecycle management: eliminate boilerplate bash scripts from SDK repos" | Verified | 2026-06-09 | Conformance repo issues | Engagement (backlog) |
| 2.14 | crates.io Trusted Publishing: GA since 2025-07-11 via `rust-lang/crates-io-auth-action` with `id-token: write` ("Required for OIDC token exchange"); "you'll need to publish your first release manually. After that, you can set up trusted publishing" — no new-crate/pending-publisher support as of the 2026-01-21 development update, which added GitLab CI/CD support, blocked `pull_request_target`/`workflow_run` triggers, and a **"Trusted Publishing Only"** crate setting ("traditional API token-based publishing is disabled") | Verified | 2026-06-09 | [Rust blog 2025-07-11](https://blog.rust-lang.org/2025/07/11/crates-io-development-update-2025-07/); [Rust blog 2026-01-21](https://blog.rust-lang.org/2026/01/21/crates-io-development-update/) | RELEASING.md, ADR-0007, release.yml |
| 2.15 | Suite 0.1.16 **server scenarios define their SUT surface in the scenario descriptions**: tools `test_simple_text`, `test_image_content`, `test_audio_content`, `test_embedded_resource`, `test_multiple_content_types`, `test_tool_with_logging`, `test_error_handling`, `test_tool_with_progress`, `test_sampling`, `test_elicitation`, `test_elicitation_sep1034_defaults`, `test_elicitation_sep1330_enums`, `json_schema_2020_12_tool`; resources `test://static-text`, `test://static-binary`, template `test://template/{id}/data`; prompts `test_simple_prompt`, `test_prompt_with_arguments`, `test_prompt_with_embedded_resource`, `test_prompt_with_image`; SEP-1330 checks expect camelCase property keys (`untitledSingle`, `titledSingle`, `legacyEnum`, `untitledMulti`, `titledMulti`); runner is **HTTP-only** (`--url`) with `--expected-failures` YAML baselines and `--output-dir` per-scenario `checks.json` verdicts | Verified | 2026-06-10 | Pinned npm tarball scenario descriptions + live runs | everything-server (M2), xtask conformance, agreement check |
| 2.16 | Conformance-repo motion: draft wire-version identifier renamed **`DRAFT-2026-v1` → `2026-07-28`** ([PR #331](https://github.com/modelcontextprotocol/conformance/pull/331), merged 2026-06-08) — README now reads: dated versions through `2025-11-25` use the stateful lifecycle, "the 2026 draft (`2026-07-28`) uses the stateless lifecycle (per-request `_meta`)" · python-sdk added as a built-in SDK runner target (PR #329, 2026-06-08) · **`0.2.0-alpha.3` is now published** — npm `alpha` dist-tag moved alpha.2 → alpha.3 between 2026-06-11 and 2026-06-12, so the scheduled alpha job now exercises the renamed draft identifier and the python-sdk runner · repo HEAD still `28f8338` on 2026-06-12 (conformance#338 open and unfixed there). **2026-07-26: our filed issue has drawn a community fix** — [conformance#340](https://github.com/modelcontextprotocol/conformance/pull/340) "Send initialized notification in DNS rebinding scenario" (author jstar0, **open**, states "Closes #338"): it captures the `Mcp-Session-Id` from the valid-Host probe and sends `notifications/initialized` afterwards, exactly resolution (a) from our filing, noting that "servers that require `notifications/initialized` after a successful `initialize` can fail or hang". #338 itself is still open. Two consequences: the `LIFE-003` row in `conformance/agreement-divergences.json` goes stale the moment a suite version carrying #340 is pinned (issue [#7](https://github.com/tomtom215/mcp-conformance/issues/7) action 3), and the PR's own scope note ("`server/discover` traffic remains unchanged") confirms the suite's dns-rebinding scenario already carries draft-revision traffic | Verified | 2026-07-26 | [Commits feed](https://github.com/modelcontextprotocol/conformance/commits/main.atom); [PR #331 diff](https://github.com/modelcontextprotocol/conformance/pull/331); [npm registry](https://registry.npmjs.org/@modelcontextprotocol%2Fconformance) | Suite tracking (alpha job), Roadmap M2.5 (our `draft-2026-07-28` gate name already matches the wire identifier; no in-repo references to the old `DRAFT-2026-v1` existed) |

| 2.17 | crates.io trusted-publishing state is externally observable: the OIDC exchange fails `400: No Trusted Publishing config found for repository tomtom215/mcp-conformance` when no crate has a config for the repo (v0.2.0 run attempt 1, 2026-06-11T13:14Z — disproving ADR-0007's 2026-06-10 "configured" claim); a config added mid-release takes effect immediately (attempt 2 published 13:37Z); the versions API attributes a token publish to a user (`published_by`: v0.1.0 → `tomtom215`) and a trusted-publishing publish to none (v0.2.0 → null). The per-crate **"Trusted Publishing Only"** toggle is **not** externally observable — only an owner can confirm it | Verified | 2026-06-11 | [Run 27348688178](https://github.com/tomtom215/mcp-conformance/actions/runs/27348688178); [crates.io versions API](https://crates.io/api/v1/crates/mcp-conformance-core) | ADR-0007 §Correction, RELEASING.md |

## 3. Official Rust SDK (rmcp) state

| # | Fact | Status | Verified | Source | Used by |
|---|------|--------|----------|--------|---------|
| 3.1 | `rmcp` latest release 1.7.0 (2026-05-13); 12,204,151 all-time downloads; repo ~3.5k stars, 38 open issues (stars/issues as of 2026-06-09). **2026-07-19: latest published is now `2.2.0`** — the line moved `1.7.0 → 1.8.0 → 2.0.0 (breaking) → 2.1.0 → 2.2.0` (crates.io sparse index `https://index.crates.io/rm/cp/rmcp`, 50 versions). **The workspace still pins `1.7.0`** (a `2.x` adoption is a breaking bump — see 3.8 for the non-breaking `1.8.0` enumNames path). Download/star/issue counts not re-verified this pass (crates.io stats API returned empty; the numbers fall under the 2026-09-07 90-day sweep). **2026-07-26: the line has entered a `3.0.0-beta` — and it is the `2026-07-28` implementation.** `3.0.0-beta.1` (2026-07-23) and `3.0.0-beta.2` (2026-07-24) are published; release notes for beta.1 list the stateless lifecycle ("the 2026-07-28 draft version is always served statelessly"), `server/discover`, the tasks extension (SEP-2663), MRTR model types (SEP-2322), and a TTL-honoring client cache (SEP-2549); beta.2 adds "omit resultType for legacy protocol sessions" (breaking) and "declare and check MSRV" (row 3.5). `max_stable` is still **2.2.0** (2026-07-08). All-time downloads now **17,354,219** (recent 8,828,814), up from 12.2M on 2026-06-09. Our workspace still pins **1.7.0**, and [ADR-0011](decisions/0011-rmcp-pin-holds-at-1-7.md) (2026-07-27) makes that a recorded decision rather than inertia: hold until the final text ships *and* a stable `3.0.x` exists | Verified | 2026-07-26 | crates.io sparse index + [API](https://crates.io/api/v1/crates/rmcp) (versions, dates, counts, 2026-07-26); [rust-sdk releases](https://github.com/modelcontextprotocol/rust-sdk/releases) (beta.1/beta.2 notes); [repo](https://github.com/modelcontextprotocol/rust-sdk) | Charter, Roadmap M2.5 (upgrade target) |
| 3.2 | License: Apache-2.0 (docs CC-BY-4.0; legacy MIT contributions noted during relicensing) | Verified | 2026-06-09 | Workspace `Cargo.toml`, LICENSE | ADR-0003, Standards |
| 3.3 | rust-sdk **already contains** a `conformance/` workspace package (internally named `mcp-conformance`, `publish = false`) with `conformance-server` and `conformance-client` binaries wired to the official suite | Verified | 2026-06-09 | [conformance/Cargo.toml](https://github.com/modelcontextprotocol/rust-sdk/tree/main/conformance) | Charter, ADR-0003 (name collision), Engagement |
| 3.4 | rust-sdk has **no everything server** (examples cover counter, memory, elicitation, prompt, task, progress, auth; `everything_stdio.rs` is a *client* example) — re-confirmed at head `52e731b` (2026-06-10): no overlapping issue or PR either | Verified | 2026-06-11 | Repo `examples/` tree at `52e731b`; issue #769; issue/PR search 2026-06-11 | Charter, Roadmap M2 |
| 3.5 | rust-sdk declares **no MSRV** — no `rust-version` in the workspace, `rmcp`, or `rmcp-macros` manifest (re-confirmed at `main` head `266f870`, 2026-06-15) — and the **measured floor of the current release is exactly 1.88**, set by a *transitive* dependency rather than rmcp's own source: with the `macros` feature, `rmcp-macros 1.7.0` → `darling = "0.23"` → `darling 0.23.0` (its only 0.23.x) declares `rust-version = 1.88.0`. Empirical re-verification (2026-06-15, a probe crate on the published `rmcp =1.7.0` with `server`+`macros`+`transport-io`, one `Cargo.lock` shared via `--locked`): `cargo +1.85.0`/`+1.87.0 check` **fail**, `cargo +1.88.0 check` **passes**. The failure is cargo's **clear, named** MSRV pre-check (`rustc 1.87.0 is not supported by … darling@0.23.0 requires rustc 1.88.0`) — **not** an opaque `E0658`, and **not** let-chains in rmcp's own source (a grep of `crates/rmcp{,-macros}/src` for `&&`-joined and `&& let` let-chains found none; the darling pre-check gates the build before rmcp's source compiles). *(Supersedes this row's 2026-06-10 "E0658 / let-chains in rmcp source" wording, disproved by the empirical re-test — see [ADR-0008 §Correction](decisions/0008-msrv-1.88.md).)* **2026-07-19: rmcp `2.2.0` (current latest) still declares no `rust-version`** (crates.io sparse index: `rust_version` absent on every published version through 2.2.0), so the MSRV-policy gap the issue draft targets persists across the `2.x` line; the *effective* 2.x floor was not re-derived this pass (the darling-transitive mechanism above was measured against 1.7.0). **RESOLVED UPSTREAM 2026-07-23 — independently of us, before we filed.** [rust-sdk#1034](https://github.com/modelcontextprotocol/rust-sdk/pull/1034) "chore: declare and check MSRV" (author jamadeo, merged 2026-07-23) adds `rust-version` to the manifests **and an MSRV job to CI**, with the rationale "This is currently 1.88 based on how we use the language now". Confirmed in published metadata, not just the PR page: `rmcp 3.0.0-beta.2` is the **first** version in the whole 52-version history whose crates.io index entry carries `rust_version: "1.88"` (every version through `2.2.0`, and `3.0.0-beta.1`, carry none). The declared value **matches the floor ADR-0008 measured empirically**. Consequences: the drafted MSRV-policy issue ([docs/reports/rust-sdk-msrv-policy-issue-draft.md](../reports/rust-sdk-msrv-policy-issue-draft.md)) is **obsolete — do not file**, and engagement backlog item 3 closes as resolved-upstream-independently. Residual (not worth a filing): the released `1.x`/`2.x` lines still declare nothing, so a consumer pinned there still gets no declared MSRV | Verified | 2026-07-26 | Local toolchain probe (1.85.0/1.87.0/1.88.0, `--locked`) against crates.io `rmcp =1.7.0`; tag/`main` manifests; darling 0.23.0 crates.io index; [Rust 1.88.0 announcement](https://blog.rust-lang.org/2025/06/26/Rust-1.88.0/); [rust-sdk#1034](https://github.com/modelcontextprotocol/rust-sdk/pull/1034) + `rmcp 3.0.0-beta.2` sparse-index `rust_version` and vendored `Cargo.toml` (`rust-version = "1.88"`), both re-fetched 2026-07-26 | ADR-0008 (MSRV 1.88), Engagement (contribution backlog item 3 — now closed) |
| 3.6 | Tier-2 process issues #690–#693 (labeling/triage, stable release, ROADMAP.md, documentation) are closed; #684 "Conformance Testing" remains open | Verified | 2026-06-09 | [Issue #684](https://github.com/modelcontextprotocol/rust-sdk/issues/684) | Engagement |
| 3.7 | Most active maintainers: 4t145 and jokemanfire; CODEOWNERS is the `@modelcontextprotocol/rust-sdk` team | Verified | 2026-06-09 | Repo CODEOWNERS, commit history | Engagement |
| 3.8 | **RESOLVED 2026-08-06 by adopting rmcp 3.1.2** (rust-sdk#905): the round-trip now preserves `enumNames`, and `tests/roundtrip.rs` was flipped from pinning the loss to pinning the fix, so an upstream regression fails loudly. Historical record follows. rmcp **dropped `enumNames` in any typed `ElicitationSchema` round-trip** (=1.7.0 and head `52e731b`): untagged `EnumSchema` tries `Single(Untitled)` before `Legacy` and the untitled struct doesn't reject unknown fields (`elicitation_schema.rs:751-758`, `600-607`, `549-566`), so legacy JSON matches it and the field is silently discarded — receiving clients lose it on every legacy elicitation, and servers lose it when building schemas via `serde_json::from_value` (rust-sdk's own conformance-server does — row 3.10). Direct typed `Legacy` construction serializes correctly (our path). Latent second bug: `enum_names` (547) lacks `skip_serializing_if`, so `Legacy(None)` emits `"enumNames": null`. Mechanism + 20-line repro failing on both versions: [#10](https://github.com/tomtom215/mcp-conformance/issues/10); **filed upstream as [rust-sdk#903](https://github.com/modelcontextprotocol/rust-sdk/issues/903)** (2026-06-11). **RESOLVED upstream: #903 closed, fixed by merged PR [rust-sdk#905](https://github.com/modelcontextprotocol/rust-sdk/pull/905)** (merged 2026-06-20 to `main`, head now `4fd4986`; author @abdouloued) — the exact fix #10 proposed: `deny_unknown_fields` on the untitled struct (now `elicitation_schema.rs:553`) plus the `enum_names` `skip_serializing_if`, with two regression tests (`..._roundtrip_preserves_enum_names`, `..._no_enum_names_omits_field`). **Not yet on crates.io** (latest published rmcp is still 1.7.0, which still drops the field), so the pinned 1.7.0 is unaffected and `tests/roundtrip.rs` keeps pinning the current loss (failing loudly the day a bumped rmcp preserves it) until we adopt a release carrying #905 (deferral `adopt-rmcp-enumnames-fix`). **2026-07-19: the fix is now on crates.io** — the published `rmcp 1.8.0` **and** `2.2.0` crates both carry #905 (`deny_unknown_fields` present in each tarball's `src/model/elicitation_schema.rs`, verified by download+grep). The workspace still pins 1.7.0 (which drops the field), so the deferral is now actionable. **Bump attempted 2026-07-19 and reverted — NOT a clean minor:** bumping `1.7.0 → 1.8.0` and flipping the round-trip assertion to *preserved* worked (the fix is real end-to-end), but 1.8.0 bundles two more behavior changes that make it a deliberate upgrade, not a hygiene bump — (a) SEP-2577 forward-deprecation of Roots/Sampling/Logging (features active + required for our 2025-11-25 surface; ~7 `#[allow(deprecated)]` sites across `mcp-everything-server` + `mcp-reference-host`), and (b) tool-argument-deserialization failures now return a **tool-error result (`isError:true`)** instead of an **MCP protocol error (`-32602`)**, breaking 3 arg-validation tests. (b) is conformance-relevant and unvalidatable without the npx suite gate, so the adoption is folded into the deliberate rmcp upgrade in the M2.5 migration (deferral `adopt-rmcp-enumnames-fix`, re-scoped) | Verified | 2026-07-19 | Repro vs crates.io `=1.7.0`; live bump attempt at `=1.8.0` (`cargo test` — enumNames preserved; 3 arg-error tests flip to `isError`; clippy deprecation errors at 9 sites); published `rmcp-1.8.0.crate` `elicitation_schema.rs` (`deny_unknown_fields`); #905 merged 2026-06-20 | Engagement ([rust-sdk#903](https://github.com/modelcontextprotocol/rust-sdk/issues/903), dossier in [#10](https://github.com/tomtom215/mcp-conformance/issues/10)) |
| 3.9 | rmcp 1.7.0 ships **under-specified dependency floors**: builds fail under `-Zminimal-versions` needing tokio-util ≥0.7.9 (`tokio_util::bytes` re-export), tokio-stream ≥0.1.1 (`wrappers`), tracing ≥0.1.41 (`#[instrument]` let-else), and sse-stream 0.2.x needs http-body-util ≥0.1.2 (`BodyDataStream`); `Implementation::from_build_env()` always reports `rmcp` (expands `env!` at its own definition site). **Re-verified 2026-06-27** against rmcp 1.7.0's *published* manifest (crates.io sparse index): the three rmcp-keyed floors are declared `tokio-util ^0.7`, `tokio-stream ^0.1`, `tracing ^0.1` — each below the needed minimum; `from_build_env` confirmed at `model.rs:1057` (undocumented; `env!("CARGO_CRATE_NAME")`/`env!("CARGO_PKG_VERSION")`; wired as the `server_info`/`client_info` default at model.rs:898/932/946). Ready-to-file draft: [docs/reports/rmcp-build-out-hygiene-draft.md](../reports/rmcp-build-out-hygiene-draft.md). **Re-checked against rmcp 2.2.0 (2026-07-19) — the floors story shrank:** declared floors are byte-unchanged (`tokio-util ^0.7`, `tokio-stream ^0.1`, `tracing ^0.1` in the 2.2.0 sparse index), **but the `tokio_util::bytes` justification is gone** — the 2.x refactor removed that usage (0 occurrences of `tokio_util::bytes` in the published `rmcp-2.2.0` source), so the tokio-util ≥0.7.9 claim no longer applies to 2.2.0. `tokio_stream::wrappers` **is still used** (2 files: `handler/client/progress.rs`, `transport/streamable_http_server/session/local.rs`), so tokio-stream ≥0.1.1 still bites; `#[instrument]` is still present (2×) so the empirical `tracing` floor is plausible but unre-confirmed (a full `-Z minimal-versions` reproduce against 2.2.0 awaits the breaking 2.x pin bump). `from_build_env` **still buggy in released 1.8.0 and 2.2.0** (`env!` at the definition site, `model.rs`). Net: the upstream floors PR is down to essentially tokio-stream at 2.2.0; `from_build_env` remains the solid piece | Verified | 2026-07-19 | Floor shims in `mcp-everything-server/Cargo.toml`; rmcp 2.2.0 crates.io index reqs; published `rmcp-2.2.0.crate` source (API-usage greps); `rmcp-1.8.0`/`2.2.0` `model.rs` (`from_build_env`) | Engagement (floors PR / docs fix — drafted), minimal-versions shims |
| 3.10 | rust-sdk's **in-repo tier audit** (`conformance/results/2026-02-25-rust-sdk-assessment.md`, "Tier Assessment: Tier 3") measured server conformance 83.3% (25/30); remediation item #1 on its Tier-1 path: "Fix 5 failing server conformance scenarios: prompts-get-with-args, prompts-get-embedded-resource, elicitation-sep1330-enums, elicitation-sep1034-defaults, dns-rebinding-protection". **Re-measured 2026-06-11** — their `conformance-server` built at head `52e731b` vs pinned suite 0.1.16, spec `2025-11-25`: **38/40**; still failing `prompts-get-with-args` ("arg1 not substituted in prompt; arg2 not substituted in prompt") and `elicitation-sep1330-enums` ("Missing or invalid enumNames array for legacy titled enum", field `legacyEnum` — same `EnumSchema` family as row 3.8). Repro: `cargo build -p mcp-conformance --bin conformance-server`; `PORT=<p> ./target/debug/conformance-server`; `npx @modelcontextprotocol/conformance@0.1.16 server --url http://127.0.0.1:<p>/mcp --spec-version 2025-11-25`. *(Update 2026-06-21: the `elicitation-sep1330-enums` root cause is fixed upstream by merged [rust-sdk#905](https://github.com/modelcontextprotocol/rust-sdk/pull/905) — row 3.8; once rust-sdk ships and rebuilds, that scenario should pass, leaving `prompts-get-with-args` as the remaining failure. The 38/40 is the 2026-06-11 snapshot at head `52e731b`, not re-measured since.)* | Verified | 2026-06-11 | In-repo audit files at `52e731b`; local build + suite run (runner `checks.json` retained) | Engagement (offer framing — [#9](https://github.com/tomtom215/mcp-conformance/issues/9)), M5 tier-gap report |
| 3.11 | rust-sdk's tracker carries a SEP-implementation wave for the `2026-07-28` window (observed 2026-06-11): #867 SEP-2164, #868 SEP-2663 (tasks), #871 SEP-2322 (MRTR), #875 SEP-2549 (TTL), #876 SEP-2468 (`iss`), #877 SEP-2350 (scope step-up), #878 SEP-2351 (RFC 8414 suffix); #684 "Conformance Testing" remains open — corroborates the row-1.5a inventory landing in SDK backlogs | Verified | 2026-06-11 | rust-sdk issue tracker (search 2026-06-11); [issue #684](https://github.com/modelcontextprotocol/rust-sdk/issues/684) | Engagement (offer timing), Roadmap M2.5 |
| 3.12 | rmcp 1.7.0's streamable-HTTP client **loses an in-flight request when its POST SSE stream closes early** and fails the suite's `sse-retry` scenario: POST response streams are wrapped "without reconnection logic" (`raw_sse_to_jsonrpc`, `streamable_http_client.rs:783`, **re-confirmed first-hand 2026-06-27 at head `eb435c6`** — `raw_sse_to_jsonrpc` at line 303 with the verbatim doc comment "without reconnection logic" at lines 301–302, applied to both POST `StreamableHttpPostResponse::Sse` arms at lines 778–779/803–804; structurally exact since `266f870`); the SEP-1699 wrapper that does honor `retry`/`Last-Event-ID` (`SseAutoReconnectStream`) guards only the standalone GET stream, which opens right after initialization (it appears only after the two `.get_stream(…)` calls, lines 584/720 — never on the POST path) — measured against suite 0.1.16 (2026-06-12, probe `checks.json` retained): `client-sse-retry-timing` FAILURE "reconnected too early (−53ms instead of 500ms)", `client-sse-last-event-id` WARNING, `tools/call` never completes. Repro: any rmcp-1.7 client vs `npx @modelcontextprotocol/conformance@0.1.16 client --scenario sse-retry`. The reference host ships the compliant dance on rmcp's public `StreamableHttpClient` seam (ADR-0009 §Amendment). **Filing re-decided 2026-06-27 — do not file the "add resumption" fix:** the `2026-07-28` draft *removes* SSE stream resumability and `Last-Event-ID` from Streamable HTTP (register 1.5d Major #9, SEP-2575 — a broken stream loses the in-flight request and the client MUST re-issue it as a new request), so the dossier's proposed POST-path reconnection is being obsoleted by the spec and upstream is implementing the removal (register 3.11). The durable residue is narrower — rmcp *hangs* on early POST-stream close instead of surfacing a re-issuable error, which the new text requires — verifiable only against the final spec + the draft suite scenarios; deferral `rmcp-sse-resumption-upstream-filing` re-dated to that window, dossier [docs/reports/rmcp-sse-resumption-dossier.md](../reports/rmcp-sse-resumption-dossier.md) carries the reconciliation. **2026-07-19: rmcp is now 2.2.0 and the mechanism persists** — `raw_sse_to_jsonrpc` / "without reconnection logic" / `SseAutoReconnectStream` all still present on `main` (rmcp has not yet removed resumability to match the draft), so both the `2025-11-25` gap and the re-decision stand | Verified | 2026-07-19 | rmcp head `eb435c6`→`main` source (SSE markers present 2026-07-19); probe run artifacts (ADR-0009 §Amendment); draft removal in register 1.5d | Engagement (upstream dossier — re-decided), reference-host `resume` |
| 3.13 | **Still unfixed upstream at 3.1.2, and the shim moved with the pin** (now `>=3.1.1, <3.2.0`): rmcp 3.1.2 continues to declare `rmcp-macros = "^3.1.2"`. **`rmcp` and `rmcp-macros` are lockstep-coupled but only caret-pinned, so Cargo can — and by default does — resolve a pair that cannot compile.** The proc-macro crate expands to calls into `rmcp`'s own internals, yet every published `rmcp` declares its companion as `rmcp-macros = "^N.M.P"` (sparse index: `^1.6.0`, `^1.7.0`, `^1.8.0`, `^2.0.0`, `^2.1.0`, `^2.2.0`, `^3.0.0-beta.1`, `^3.0.0-beta.2`), which admits a *newer* macro crate against an older library. Demonstrated first-hand (2026-07-26, `cargo update -p rmcp-macros --precise 1.8.0 && cargo check -p mcp-everything-server`): `rmcp 1.7.0` + `rmcp-macros 1.8.0` fails with **E0425 at five `#[tool]` sites** — 1.8.0's expansion calls `rmcp::handler::server::common::schema_for_input`, which 1.7.0 does not export (1.7.0's `common.rs` has `schema_for_type`, `schema_for_empty_input`, `schema_for_output`; `schema_for_input` is added in 1.8.0). A committed `Cargo.lock` hides this; **anything that resolves fresh hits it**, which is exactly what turned our scheduled *Dependency floors* job red for three consecutive weeks (2026-07-06 / -13 / -20 — the gate regenerates the lock, and `rmcp-macros 1.8.0` published 2026-06-23). Scope, stated honestly: the defect is **not currently reproducible inside the 2.x line** (published `rmcp 2.0.0` and `2.2.0` both export the full `schema_for_*` set their macros emit), so today it is a *latent* hazard there — but the caret declaration is **unfixed at `3.0.0-beta.2`**, so the next macro-side API addition re-arms it. Upstream fix is one line: exact-pin the companion crate (`rmcp-macros = "=N.M.P"`), the `serde`/`serde_derive` convention. Our side: a documented **ceiling shim** (`rmcp-macros = ">=1.7.0, <1.8.0"`) in `crates/mcp-everything-server/Cargo.toml`, which must move with the rmcp pin at the M2.5 upgrade and fails loudly if it does not | Verified | 2026-07-26 | Local repro (`cargo update --precise` + `cargo check`, this workspace, 2026-07-26); published `rmcp-{1.7.0,1.8.0,2.0.0,2.2.0}` and `rmcp-macros-{1.7.0,1.8.0,2.0.0,2.2.0}` sources (symbol greps); crates.io sparse index (`rmcp-macros` reqs, all versions); [failing job](https://github.com/tomtom215/mcp-conformance/actions/runs/29723184969) | Engagement (backlog item 11 — the strongest buildable-now M4 candidate), floors gate |
| 3.14 | **rmcp cut a stable `3.0.0` on 2026-07-28 — the same day the specification text shipped — closing [ADR-0011](decisions/0011-rmcp-pin-holds-at-1-7.md)'s second condition** (the first closed that morning, row 1.5h). The line has moved four times since and is **not** sitting still: `3.0.0` (2026-07-28), `3.0.1` (2026-07-29), `3.1.0` (2026-07-31), `3.1.1` (2026-08-05). Superseding row 3.1's `2.2.0` snapshot and row 1.5h's same-day reading of `3.0.0-beta.4` / `max_stable 2.2.0` — that reading was taken at 20:20 UTC on 2026-07-28 and `3.0.0` published later the same day, so it was accurate when verified and stale within hours. **MSRV is declared `1.88` on every `3.x` release** (and from `3.0.0-beta.2` on), exactly the floor [ADR-0008](decisions/0008-msrv-1.88.md) measured empirically, so adoption costs no MSRV movement — the gap row 3.5 recorded (rust-sdk declaring no MSRV at all) is closed in the published manifests. **The decisive adoption fact: `3.x` keeps the `2025-11-25` surface and still defaults to it.** Read first-hand in the unpacked `rmcp-3.1.1` crate, `src/model.rs`: `ProtocolVersion` declares `V_2025_06_18`, `V_2025_11_25` and `V_2026_07_28` (l.170-172), lists all three in `SUPPORTED` (l.184-186), parses all three (l.214-216), and `LATEST` is **still** `V_2025_11_25` (l.175) with `STANDARD_HEADERS = V_2026_07_28` (l.178) marking the new revision as opt-in. So the upgrade does not trade the standing 40/40 `2025-11-25` gate for an ungated window while the registry lacks `2026-07-28` entries — exactly one variable moves at a time, which is the sequencing ADR-0011 required. **Reading correction:** that ADR's §Alternatives cited `3.0.0-beta.2`'s `LATEST = V_2025_11_25` as a signal *the ecosystem agrees the revision is not live*; the same constant at 3.1.1 now means something else entirely — rmcp treats `2025-11-25` as the default target and `2026-07-28` as opt-in. **Row 3.13's caret defect is unchanged upstream:** `rmcp 3.1.1` still declares `rmcp-macros = "^3.1.1"`, so the ceiling shim in `crates/mcp-everything-server/Cargo.toml` stays necessary and must move to the `3.1` minor with the pin | Verified | 2026-08-06 | Sparse index [`index.crates.io/rm/cp/rmcp`](https://index.crates.io/rm/cp/rmcp) (59 versions; `rust_version` and `rmcp-macros` req read per version); crates.io `/versions` API for publish dates; `rmcp-3.1.1.crate` fetched from static.crates.io, unpacked and read first-hand | ADR-0011 (**condition 2 met — pin released**), Roadmap M2.5 (the upgrade half), deferral `adopt-rmcp-enumnames-fix` |
| 3.15 | **Measured migration cost `1.7.0 → 3.1.1`, attempted first-hand on 2026-08-06 and deliberately reverted** — the working tree stays green at 1.7.0; this row is the survey, not a regression. Pins moved cleanly (`rmcp = ">=3.1.1, <4"`, shim `>=3.1.1, <3.2.0`); `cargo update -p rmcp` resolves with no conflict, adding `base64 0.23.1` and moving `darling 0.23 → 0.24`. **All five features we use survive** (`server`, `macros`, `transport-io`, `transport-streamable-http-server`, `elicitation`), and `transport-streamable-http-server` still implies `transport-streamable-http-server-session`, so the `2025-11-25` session surface remains available. **25 workspace errors, of which 10 are a pure rename layer** dispatched by one `sed`: `Content`→`ContentBlock` (identical `text`/`image`/`audio`/`resource` constructors), `PromptMessageRole`→`Role`, `PrimitiveSchema`→`PrimitiveSchemaDefinition`, `CreateElicitationRequestParams`→`ElicitRequestParams`, `CreateElicitationResult`→`ElicitResult`. **The remaining 22 (15 server + 7 host) are structural:** the `Annotated<Raw*>` wrapper pattern is gone (`AnnotateAble`, `RawResource`, `RawResourceTemplate`, `RawContent`, `RawAudioContent` all deleted; `Resource`/`ResourceTemplate` are now flat structs carrying `annotations`); list results gained required `result_type`/`ttl_ms`/`cache_scope` (SEP-2322, SEP-2549); handler returns became MRTR unions (`CallToolResult`→`CallToolResponse`, `ReadResourceResult`→`ReadResourceResponse`); four params structs are now `#[non_exhaustive]` (E0639) and five matches lost exhaustiveness (E0004). **The wire question resolves in rmcp's favour, and is why the upgrade is safe at all:** `result_type` is `Option<ResultType>` with `skip_serializing_if = "Option::is_none"`, and rmcp's own doc says "the server handler clears the field when responding to peers that negotiated an older version" — 3.x is *designed* to serve a compliant `2025-11-25` surface, so the new revision's fields do not leak into old-revision responses. **One capability is genuinely removed, and it does not touch the gate:** `notifications/elicitation/complete` and `ElicitationResponseNotificationParam` are gone (2026-07-28 deleted both — row 1.5d Minor #11), with `Peer::notify_url_elicitation_completed` and `ClientHandler::on_url_elicitation_notification_complete`. Our `test_url_elicitation` and the host's handler use them, but **no suite scenario exercises URL mode** (recorded in `interactive.rs`'s own doc comment; `conformance/expected-failures.yaml` is empty both ways at 40/40), so what is at risk is row 2.10's TypeScript-everything-server *parity* claim, not the gate. `ServerNotification::CustomNotification` + `Peer::send_notification` is a viable way to keep the wire behaviour without SDK support. **Four decisions gate the landing, which is why this is not a bump:** (1) MRTR — do handlers always return `Complete`, or does the everything server exercise `input_required`; (2) URL elicitation — re-implement over `CustomNotification`, or drop the tool and amend row 2.10; (3) the 1.8.0 tool-argument-deserialization change (tool-error result vs `-32602`), still unresolved and conformance-relevant; (4) the SEP-2577 `#[allow(deprecated)]` sites for Roots/Sampling/Logging, all still REQUIRED on our `2025-11-25` surface | Verified | 2026-08-06 | First-hand `cargo update -p rmcp` + `cargo build --workspace --all-features` at 3.1.1, full error inventory captured before revert; `rmcp-3.1.1` sources read directly — `src/model.rs` (`result_type` serde attrs and doc), `src/model/{content,resource,annotated,elicitation_schema}.rs`, `src/service.rs` (`send_notification`) | Roadmap M2.5 (upgrade half — cost now measured), ADR-0011 (execution, not the decision), deferral `adopt-rmcp-enumnames-fix` |
| 3.16 | **The rmcp 1.7.0 -> 3.1.2 upgrade landed, and a before/after wire diff proves what moved.** Method: capture every tapped session at 1.7.0, upgrade, re-capture, compare normalized transcripts as a multiset (session UUIDs, ephemeral port and dates erased). **The instrument was calibrated first** — two consecutive 1.7.0 runs produced *zero* differences across all 30 server and 4 client sessions, so any post-upgrade difference is signal, not run-to-run noise. **Exactly three effects account for every difference.** (1) *Schema metadata no longer leaks:* five `inputSchema`s and one `outputSchema` — precisely the tools taking a `Parameters<T>` args struct — lost the schemars-derived top-level `title` and `description`. Those were rustdoc artifacts on the wire (`add`'s read `"Arguments for [`EverythingServer::add`]."`); per-property descriptions and the tool's own `description` are untouched, so this is a hygiene improvement, not a loss. (2) *`clientInfo.version` tracks the SDK*, `1.7.0` -> `3.1.2` — not a new fact but row 3.9's `from_build_env` bug re-observed: rmcp reports its own version as the app's identity. (3) *One session became nondeterministic:* `server-sse-multiple-streams` (concurrent `tools/list` on multiple POST streams) interleaves its responses differently run to run at 3.1.2, proven by diffing two 3.1.2 runs against each other; at 1.7.0 it was stable. Response ordering across independent concurrent requests is unconstrained by the spec and the scenario passes, but that session can no longer be byte-pinned. **With (1) and (2) normalized away, 29/30 server sessions and 4/4 client sessions are byte-identical**, the sole exception being the session (3) proves is nondeterministic. The 40/40 server + 4/4 client gate stayed green with both expected-failure lists still empty, and the agreement check still reports zero unexplained divergence | Verified | 2026-08-06 | `cargo xtask conformance` at 1.7.0 (x2, calibration) and at 3.1.2 (x2, determinism); tapped traces under `target/conformance/{tap,client-tap}` diffed field-by-field; per-tool schema comparison across all 18 tools | Roadmap M2.5, ADR-0011 (the measured half of "measured rather than asserted") |
| 3.17 | **Our tool-argument validation was reporting the wrong error class, and the upgrade corrected it.** rmcp 1.7.0 answered a `tools/call` whose arguments fail the tool's own `inputSchema` with an MCP protocol error (`-32602`); 1.8.0 changed that to a tool-execution result with `isError: true`, and this workspace pinned the old behaviour in three tests rather than adopting it (deferral `adopt-rmcp-enumnames-fix`). **rmcp is right and we were wrong.** The `2025-11-25` tools page splits the mechanisms by *what* failed: protocol errors cover "Unknown tools, Malformed requests (requests that fail to satisfy CallToolRequest schema), Server errors", while tool execution errors (`isError: true`) cover "API failures, **Input validation errors**, Business logic errors". `{"a": 1}` for a tool needing `a` and `b` is a perfectly valid `CallToolRequest` — it fails only `add`'s `inputSchema` — so it is an input validation error. Three tests were inverted deliberately (missing required argument, wrong-typed argument, unknown enum variant); two behaviours were **kept** as protocol errors because the spec puts them there: unknown tools (category 1, and the spec's own worked example) and missing *prompt* arguments, since `GetPromptResult` has no `isError` channel at all. The 40/40 gate is green either way — the suite does not grade this distinction — so the correction was made on the spec text, not on the runner's verdicts | Verified | 2026-08-06 | [2025-11-25 tools §Error Handling](https://modelcontextprotocol.io/specification/2025-11-25/server/tools) (raw fetched 2026-08-06, quoted verbatim above); `tests/roundtrip.rs` before/after; `cargo xtask conformance` green in both states | Roadmap M2.5, deferral `adopt-rmcp-enumnames-fix` (closed) |

## 4. Security

| # | Fact | Status | Verified | Source | Used by |
|---|------|--------|----------|--------|---------|
| 4.1 | CVE-2026-42559 / GHSA-89vp-x53w-74fx: rmcp's streamable-HTTP server transport did not validate the `Host` header → DNS rebinding. Severity High, CVSS 8.8, CWE-346/CWE-350. Affected < 1.4.0, patched 1.4.0. Published 2026-04-29 (per the GHSA page; an earlier register revision recorded 2026-05-06, and a third-party research pass claimed 2026-05-14 — the advisory page is authoritative), credited to JLLeitschuh | Verified | 2026-06-09 | [GHSA-89vp-x53w-74fx](https://github.com/modelcontextprotocol/rust-sdk/security/advisories/GHSA-89vp-x53w-74fx) | Security model |
| 4.2 | Fix shape: `validate_dns_rebinding_headers()` on all requests (403 on miss); `StreamableHttpServerConfig::allowed_hosts` defaulting to `["localhost", "127.0.0.1", "::1"]`; `with_allowed_hosts(…)` builder | Verified | 2026-06-09 | Advisory + rmcp release notes | Security model, Architecture |
| 4.3 | **No `rmcp`-keyed RustSec advisory exists** (`rustsec.org/packages/rmcp.html` → 404, re-checked 2026-06-14), so direct `rmcp < 1.4.0` dependents get no `cargo audit` warning. The CVE is **not** absent from the DB, though: the dynoxide-rs advisory **RUSTSEC-2026-0140** (issued 2026-05-13) aliases **both CVE-2026-42559 and rmcp's own GHSA-89vp-x53w-74fx** (plus its own GHSA-fvh2-gm75-j4j7), because dynoxide-rs was vulnerable transitively through rmcp (fixed by bumping rmcp 1.1.1→1.6.0). That over-claims — NVD scopes the CVE to *RMCP* and GHSA-89vp-x53w-74fx is rmcp's — so a clean `rmcp`-keyed advisory would **collide on those aliases**; closing the gap is a maintainer-reconciliation matter, not a drop-in PR (drafted/blocked: [docs/reports/rmcp-cve-2026-42559-rustsec-advisory-draft.md](../reports/rmcp-cve-2026-42559-rustsec-advisory-draft.md)). **Re-checked 2026-07-26 against advisory-db `main`: unchanged, still blocked.** No `crates/rmcp/RUSTSEC-*.md` exists, and `crates/dynoxide-rs/RUSTSEC-2026-0140.md` still carries `package = "dynoxide-rs"` with `aliases = ["CVE-2026-42559", "GHSA-89vp-x53w-74fx", "GHSA-fvh2-gm75-j4j7"]` — the alias collision that blocks a clean `rmcp`-keyed advisory is intact, so the reconciliation issue (not a PR) remains the only route | Verified | 2026-07-26 | [RUSTSEC-2026-0140](https://rustsec.org/advisories/RUSTSEC-2026-0140.html); [advisory-db#2852](https://github.com/rustsec/advisory-db/pull/2852); [GHSA-89vp-x53w-74fx](https://github.com/modelcontextprotocol/rust-sdk/security/advisories/GHSA-89vp-x53w-74fx); [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-42559); advisory-db `main` raw fetch of `crates/dynoxide-rs/RUSTSEC-2026-0140.md` + absence of `crates/rmcp/` (2026-07-26) | Engagement (contribution backlog) |
| 4.4 | The "CSRF" label sometimes attached to this CVE belongs to a different advisory (GHSA-fvh2-gm75-j4j7, npm `dynoxide`); rmcp's advisory is DNS rebinding only | Verified | 2026-06-09 | GitHub advisory DB | Security model (precision) |

## 5. Prior art and adjacent tooling

No existing tool — in any language — validates recorded MCP traces against the spec's
normative requirements, and no Rust everything server exists (5.1–5.4 are the closest
neighbors). Honest accounting:

| # | Tool | What it is | Scale (2026-06-09) | Overlap with us |
|---|------|------------|--------------------|-----------------|
| 5.1 | `tooltest` / `tooltest-core` | "CLI conformance testing for MCP servers" | 162 / 253 downloads | Live server probing; no trace validation, no requirement registry |
| 5.2 | `mcp-tester` | MCP server testing tool (lib + CLI) | 510 downloads | Same category as 5.1 |
| 5.3 | `mcp-wallfacer` / `wallfacer-core` | Runtime fuzzing and invariant testing for MCP servers | 189 / 198 downloads | Fuzzing, not conformance |
| 5.4 | `agentox` (AgentOx) | MCP security and conformance auditor | 88–153 downloads; 6 stars | Security-audit flavored |
| 5.5 | `mcp-probe` (markndg), `mcptest` (soapbucket) | Contract/compliance test runners, both created 2026-05-13 | 0–2 stars | Early, unestablished |
| 5.6 | `mcplint` | Testing/fuzzing/security scanning | small | Lint flavored |
| 5.7 | Alternative Rust SDKs: `rust-mcp-sdk` (153k downloads), `pmcp` (66.7k, "full TypeScript SDK compatibility"), `turbomcp-protocol`, `mcp-protocol-sdk` | SDKs, not conformance tools | various | Potential validator *users*, not competitors |
| 5.8 | `mcp-host` crate | "Production-grade MCP host" (seuros/mcphost-rs), active, 33 releases | 1,430 downloads | Name collision only — forced our host crate's name (ADR-0003) |
| 5.9 | `mcp-spec` crate | Name reserved 2025-02-27 by MCP maintainers (dsp-ant, baxen) for rust-sdk | 0.1.0 placeholder | Signals official intent around spec-types naming; we stay clear |

Crate-name availability snapshot (full table and decision in
[ADR-0003](decisions/0003-crate-naming.md)): `mcp-conformance`, `mcp-conformance-core`,
`mcp-trace-validator`, `mcp-everything-server`, `mcp-reference-host`, `mcp-tck`, `mcp-test`,
`mcp-validator` all returned 404 (available) from the crates.io API on 2026-06-09;
`mcp-host` and `praxis` are taken. Names in this space move fast — `mcp-host` went from
unregistered to 33 releases in five months.

## 6. Adjacent product context

Facts that motivate the non-goals in the [charter](00-charter.md) and
[ADR-0002](decisions/0002-product-scope.md):

| # | Fact | Status | Verified | Source |
|---|------|--------|----------|--------|
| 6.1 | Official Claude API client SDKs: Python, TypeScript, C#, Go, Java, PHP, Ruby — no Rust | Verified | 2026-06-09 | [platform.claude.com client SDKs](https://platform.claude.com/docs/en/api/client-sdks) |
| 6.2 | Anthropic acquired Stainless (announced 2026-05-18), which "powered the generation of every official Anthropic SDK"; hosted Stainless products including the SDK generator are winding down | Verified | 2026-06-09 | [Anthropic](https://www.anthropic.com/news/anthropic-acquires-stainless); [Stainless blog](https://www.stainless.com/blog/stainless-is-joining-anthropic/); [TechCrunch](https://techcrunch.com/2026/05/18/anthropic-has-acquired-the-dev-tools-startup-used-by-openai-google-and-cloudflare/) |
| 6.3 | Claude Agent SDK ships in Python and TypeScript only; existing community Rust "ports" wrap the `claude` CLI as a subprocess | Verified | 2026-06-09 | [Agent SDK overview](https://code.claude.com/docs/en/agent-sdk/overview); crates' own docs |
| 6.4 | agentgateway: Rust agentic proxy, Linux Foundation project (Solo.io contribution, 2025-08-25), hosted by the Agentic AI Foundation; 3,182 stars | Verified | 2026-06-09 | [Repo](https://github.com/agentgateway/agentgateway); [LF press](https://www.prnewswire.com/news-releases/linux-foundation-welcomes-agentgateway-project-to-accelerate-ai-agent-adoption-while-maintaining-security-observability-and-governance-302534106.html) |

Implication, in one line per non-goal: a Rust Messages-API client can be obsoleted by
Anthropic at will (6.1, 6.2); an Agent SDK port is a subprocess wrapper around a vendor
product (6.3); the Rust proxy/gateway space is owned by a Linux Foundation project with three
orders of magnitude more adoption (6.4). Conformance tooling has the opposite profile: every
spec revision *increases* its value, and 2.5/2.9 make it structurally load-bearing.
