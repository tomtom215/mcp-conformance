{
  "revision": "2025-11-25",
  "requirements": [
    {
      "id": "LIFE-001",
      "level": "MUST",
      "actor": "client",
      "source": {
        "section": "basic/lifecycle#initialization",
        "quote": "The initialization phase MUST be the first interaction between client and server."
      },
      "checks": [
        "lifecycle.first-interaction-initialize"
      ]
    },
    {
      "id": "LIFE-002",
      "level": "MUST",
      "actor": "client",
      "source": {
        "section": "basic/lifecycle#initialization",
        "quote": "The client MUST initiate this phase by sending an `initialize` request containing: Protocol version supported; Client capabilities; Client implementation information"
      },
      "checks": [
        "lifecycle.initialize-params"
      ]
    },
    {
      "id": "LIFE-003",
      "level": "MUST",
      "actor": "client",
      "source": {
        "section": "basic/lifecycle#initialization",
        "quote": "After successful initialization, the client MUST send an `initialized` notification to indicate it is ready to begin normal operations"
      },
      "checks": [
        "lifecycle.initialized-notification"
      ]
    },
    {
      "id": "LIFE-004",
      "level": "SHOULD NOT",
      "actor": "client",
      "source": {
        "section": "basic/lifecycle#initialization",
        "quote": "The client SHOULD NOT send requests other than pings before the server has responded to the `initialize` request."
      },
      "checks": [
        "lifecycle.client-requests-before-init-response"
      ]
    },
    {
      "id": "LIFE-005",
      "level": "SHOULD NOT",
      "actor": "server",
      "source": {
        "section": "basic/lifecycle#initialization",
        "quote": "The server SHOULD NOT send requests other than pings and logging before receiving the `initialized` notification."
      },
      "checks": [
        "lifecycle.server-requests-before-initialized"
      ]
    },
    {
      "id": "LIFE-006",
      "level": "MUST",
      "actor": "server",
      "source": {
        "section": "basic/lifecycle#version-negotiation",
        "quote": "If the server supports the requested protocol version, it MUST respond with the same version. Otherwise, the server MUST respond with another protocol version it supports."
      },
      "checks": [
        "lifecycle.initialize-result-version"
      ]
    },
    {
      "id": "LIFE-007",
      "level": "MUST",
      "actor": "client",
      "source": {
        "section": "basic/lifecycle#version-negotiation",
        "quote": "In the `initialize` request, the client MUST send a protocol version it supports."
      },
      "checks": [
        "lifecycle.initialize-protocol-version"
      ]
    },
    {
      "id": "LIFE-008",
      "level": "MUST",
      "actor": "both",
      "source": {
        "section": "basic/lifecycle#operation",
        "quote": "Both parties MUST: Respect the negotiated protocol version"
      },
      "exclusion": "After negotiation, neither stdio nor HTTP message payloads carry a per-message protocol version, so respect is not observable in message traffic. The one wire artifact — the HTTP `MCP-Protocol-Version` header — is judged under TRAN-017 and TRAN-018."
    },
    {
      "id": "LIFE-009",
      "level": "MUST",
      "actor": "both",
      "source": {
        "section": "basic/lifecycle#operation",
        "quote": "Both parties MUST: Only use capabilities that were successfully negotiated"
      },
      "checks": [
        "lifecycle.negotiated-capabilities-only"
      ]
    }
  ]
}
