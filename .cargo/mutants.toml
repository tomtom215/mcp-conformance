# SPDX-License-Identifier: MIT
# Copyright 2026 Tom F. (https://github.com/tomtom215)
#
# cargo-mutants configuration — mutation testing.
# Gate: zero surviving mutants in every shipped crate
# (docs/plan/04-engineering-standards.md). PRs run diff-scoped (--in-diff);
# the scheduled workflow runs the full sweep.
#
# Location matters: cargo-mutants reads `.cargo/mutants.toml`, not a root-level
# `mutants.toml` — a root file is silently ignored, settings and all.
#
# Install:  cargo install cargo-mutants --locked
# Run:      cargo mutants --workspace

# Generous multiplier so slower CI runners don't misreport slow-killed mutants
# as timeouts; the base timeout is auto-detected from a clean `cargo test`.
timeout_multiplier = 3.0

# Floor: no mutant gets less than 30s even when the baseline is very fast
# (this workspace's unit tests run in milliseconds, which would otherwise
# produce a hair-trigger timeout). Genuine hangs are bounded by the
# multiplier-derived timeout.
minimum_test_timeout = 30

# xtask is dev-only orchestration glue; mutating it proves nothing about the
# shipped crates and doubles sweep time.
exclude_globs = ["xtask/**/*.rs"]

# Equivalent mutant, excluded with justification rather than left "missed":
# in session_id_echoed, client requests are compared against the seq of the
# server response event that assigned the session ID. One event owns one seq,
# and the assignment event is the server's — a client request can never carry
# `seq == assigned_seq`, so `<` and `<=` are observably identical and no test
# can separate them.
exclude_re = [
    "replace < with <= in session_id_echoed",
    # Equivalent mutant: in split_frame, `ci < li` compares the byte offsets
    # of an LF ("\n\n") match and a CRLF ("\r\n\r\n") match. Equality would
    # require the same byte to begin both boundaries — to be '\n' and '\r'
    # at once — so `<` and `<=` are observably identical and no test can
    # separate them.
    "replace < with <= in split_frame",
]
